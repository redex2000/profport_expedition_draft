- page_title "Список экспедиций"

- if policy(Expedition).new?
  .float-right
    = link_to 'Создать', new_expedition_path, class: 'btn btn-info', remote: true

%table.table.table-hover#expeditions
  %thead
    %tr
      %th Название
      %th
      %th

  %tbody= render partial: 'expedition', collection: @expeditions
